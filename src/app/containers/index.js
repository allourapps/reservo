import React, { Component } from 'react';
import Root from './root';
import './styles.scss';

export default class App extends Component {
    render() {
        return (
            <Root/>
        );
    }
}
